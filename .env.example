# 실제 값이 채워진 .env 파일은 절대 git에 커밋하지 않는다.
# 이 파일은 필요한 환경변수 목록만 보여주는 템플릿이다.

# 서버
PORT=3000
NODE_ENV=development

# DB (MySQL/MariaDB)
DB_HOST=localhost
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=
DB_NAME=tiny_secondhand

# 세션
SESSION_SECRET=

# 기타
BCRYPT_SALT_ROUNDS=12
